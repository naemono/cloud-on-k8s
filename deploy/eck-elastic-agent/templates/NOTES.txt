
1. Check Agent status
  $ kubectl get agent -n {{ .Release.Namespace }}

2. Check Agent pod status
  $ kubectl get pods --namespace={{ .Release.Namespace }} -l agent.k8s.elastic.co/name={{ include "elasticagent.fullname" . }}
